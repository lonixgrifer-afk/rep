meta:
  environment: python
  toolchain: pip

build:
  requirements: requirements.txt

run:
  script: python max.py
  persistence:
    - target: /app/sessions
    - target: /app/users.json
    - target: /app/invoices.json
    - target: /app/events.json
